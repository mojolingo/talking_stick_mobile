# Talking Stick Client for iOS

## Building

* Install NodeJS
* Install Cordova: `npm install -g cordova`
* [Optional] Install the helper for launching the iOS simulator: `npm install -g ios-sim`
* Build: `cordova build ios`
* Test: `cordova emulate ios`
